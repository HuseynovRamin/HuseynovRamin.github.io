import React from "react";
import ReactDOM from "react-dom/client";
import App from "./App";
import "./index.css"; // Make sure to have Tailwind CSS setup her

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(<App />);
